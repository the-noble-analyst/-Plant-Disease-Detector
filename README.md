# -Plant-Disease-Detector
End-to-end computer vision project using PyTorch and ResNet18 to detect plant leaf diseases from images. Built a full ML pipeline with data prep, augmentation, transfer learning, fine-tuning, and error analysis. Achieved ~98% test accuracy and deployed a Streamlit app for real-time predictions.
